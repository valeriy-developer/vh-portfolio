import React from "react";

const ThemeRevealer = () => {
  return (
    <div
      data-theme-revealer
      className="bg-primary easing fixed inset-0 z-[9999] origin-bottom scale-y-0"
    />
  );
};

export default ThemeRevealer;
